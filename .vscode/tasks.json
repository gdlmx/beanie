{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start mongo server",
            "type": "shell",
            "command": "docker run --rm --name dev-mongo -p 27017:27017 -d mongo",
            "problemMatcher": []
        }
    ]
}